// examples/demo.ltl — runnable demo for ltl_6502_emu

import std::io
use ltl_6502_emu as lib

fn main() {
    io::println("ltl_6502_emu demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}