// ltl-6502-emu/main.ltl
//
// MOS 6502 emulator with cycle-accurate instruction timing.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-6502-emu v0.1.0")
    io::println("MOS 6502 emulator with cycle-accurate instruction timing.")
    0
}
